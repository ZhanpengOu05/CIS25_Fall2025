item.h is the header file for item
item.cpp is for implementation file, and can give uses to access classes structure
Main.cpp is the body of the program, it sets up datas like "tool name = "Wrench"; tool.quantity = 10;
