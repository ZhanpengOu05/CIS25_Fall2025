I created functions.h giving the direction where the file is.
Main.cpp is codes for the MENU, also a loop that can keep reating until the user exits.
Function.cpp is for function declare in fuction.h
